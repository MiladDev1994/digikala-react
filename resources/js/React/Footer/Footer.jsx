import React from 'react';
import styles from './Footer.module.css';

const Footer = () => {
    return (
        <div className={'w-100 bg-dark'}>
            <div className={`${styles.box} m-auto`}>

            </div>
        </div>
    );
};

export default Footer;
